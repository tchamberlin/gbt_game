{
  "name": "gbt_game",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run server.ts",
    "build": "rm -rf dist && mkdir -p dist/docs && bun build src/main.ts --outfile dist/bundle.js --minify && cp index.html gbt.png gbt.svg dist/ && cp -r claude_code_log/* dist/docs/",
    "bundle": "bun build src/main.ts --outfile bundle.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
